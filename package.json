{
  "name": "slp-backend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared/*",
    "packages/*"
  ],
  "scripts": {
    "dev:all": "bash scripts/start.sh",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "type-check": "tsc --noEmit --project tsconfig.base.json && npm run type-check --workspaces --if-present",
    "lint": "eslint 'packages/**/src/**/*.ts' --max-warnings=0",
    "format": "prettier --write 'packages/**/*.{ts,json}'",
    "test": "jest --config jest.config.ts",
    "test:integration": "jest --config jest.integration.config.ts",
    "test:e2e": "jest --config jest.e2e.config.ts",
    "test:newman": "node scripts/newman-run.js",
    "test:newman:cell": "node scripts/newman-cell-service.js",
    "test:newman:raw": "newman run postman/CMP_Backend.postman_collection.json --environment postman/CMP_Local.postman_environment.json --delay-request 500 --reporters cli,htmlextra --reporter-htmlextra-export postman/newman-report.html"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^9.2.1",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.1",
    "prettier": "^3.0.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0",
    "wait-on": "^9.0.5"
  }
}
