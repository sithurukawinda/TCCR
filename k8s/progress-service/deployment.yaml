# Template — copy to k8s/<service-name>/deployment.yaml and fill in progress-service and 3005
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cmp-progress-service
  labels:
    app: cmp-progress-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cmp-progress-service
  template:
    metadata:
      labels:
        app: cmp-progress-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: progress-service
          image: REGISTRY/cmp-progress-service:latest
          ports:
            - containerPort: 3005
          envFrom:
            - secretRef:
                name: cmp-secrets
          env:
            - name: progress-service
              value: progress-service
            - name: 3005
              value: "3005"
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3005
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu:    100m
              memory: 128Mi
            limits:
              cpu:    500m
              memory: 512Mi
