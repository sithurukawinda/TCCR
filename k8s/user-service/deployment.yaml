# Template — copy to k8s/<service-name>/deployment.yaml and fill in user-service and 3002
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cmp-user-service
  labels:
    app: cmp-user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cmp-user-service
  template:
    metadata:
      labels:
        app: cmp-user-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: user-service
          image: REGISTRY/cmp-user-service:latest
          ports:
            - containerPort: 3002
          envFrom:
            - secretRef:
                name: cmp-secrets
          env:
            - name: user-service
              value: user-service
            - name: 3002
              value: "3002"
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu:    100m
              memory: 128Mi
            limits:
              cpu:    500m
              memory: 512Mi
